"""MathModel QA Engine – paper preprocessing toolkit.

This package focuses on deterministic preprocessing:
- Read `papers.csv`
- Split each paper Markdown into heading blocks
- Classify blocks into a fixed set of sections
- Emit LlamaIndex nodes (or JSONL) with rich metadata
"""

