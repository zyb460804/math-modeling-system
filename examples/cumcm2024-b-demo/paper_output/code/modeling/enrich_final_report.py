from __future__ import annotations

from pathlib import Path

import pandas as pd


ROOT = Path.cwd()
OUT = ROOT / "paper_output"
TABLES = OUT / "tables"


def md_table(df: pd.DataFrame, columns: list[str], max_rows: int = 10) -> str:
    view = df[columns].head(max_rows)
    lines = [
        "| " + " | ".join(columns) + " |",
        "| " + " | ".join("---" for _ in columns) + " |",
    ]
    for _, row in view.iterrows():
        values = []
        for col in columns:
            value = row[col]
            if isinstance(value, float):
                value = f"{value:.4f}"
            values.append(str(value).replace("|", "/").replace("\n", " "))
        lines.append("| " + " | ".join(values) + " |")
    return "\n".join(lines)


def main() -> int:
    q1 = pd.read_csv(TABLES / "q1_sampling_plan.csv")
    q2 = pd.read_csv(TABLES / "q2_best_policy.csv")
    q3 = pd.read_csv(TABLES / "q3_best_policy.csv")
    q4_q2 = pd.read_csv(TABLES / "q4_q2_robust_policy.csv")
    q4_q3 = pd.read_csv(TABLES / "q4_q3_robust_policy.csv")

    reject = q1.iloc[0]
    accept = q1.iloc[1]
    q3_best = q3.iloc[0]

    report = f"""# 生产过程中的决策问题

## 摘要

本文围绕电子产品生产过程中的检测、装配、拆解和退换货损失决策，建立了由抽样检验、期望收益优化和稳健决策组成的综合模型。问题一将供应商批次次品率判断转化为二项分布下的单侧置信界问题，通过枚举样本量和次品阈值，给出满足95%信度拒收和90%信度接收要求的最小抽样方案。问题二把零配件1检测、零配件2检测、成品检测和不合格成品拆解表示为四个0-1变量，构造单位产品期望利润函数，对表1六种情形逐一枚举全部16种策略。问题三进一步扩展到两道工序、八个零配件、三个半成品和一个成品的分层结构，先计算每个半成品的有效成本和通过概率，再在成品层枚举全流程策略。问题四考虑次品率不是已知常数，而是来自抽样检测的估计值，使用Beta后验扰动和10%分位收益准则重新计算稳健策略。

计算结果表明，当标称次品率为10%时，95%信度拒收的规则为抽检 {int(reject['n'])} 件且次品数满足 {reject['threshold']}；90%信度接收的规则为抽检 {int(accept['n'])} 件且次品数满足 {accept['threshold']}。问题二中，情况1至情况5均倾向于保留拆解决策，情况6由于拆解费用达到40元，最优策略转为不拆解。问题三的最优策略为“{q3_best['policy']}”，单位产品期望利润为 {q3_best['expected_profit']:.2f} 元/件。问题四说明，若次品率来自抽样估计，策略评价不能只看名义期望利润，还需要关注后验扰动下的低分位收益。

**关键词：** 抽样检验；二项分布；期望利润；检测决策；拆解决策；稳健优化

## 1 问题重述

某企业生产一种畅销电子产品。生产前需要采购两种零配件，并将两种零配件装配为成品。若任一零配件不合格，则成品一定不合格；即使两个零配件均合格，装配过程也可能产生不合格成品。企业在生产过程中面临三类关键决策：是否检测零配件、是否检测成品、是否拆解检测出的不合格品。若不合格品流入市场，企业还需承担无条件调换带来的损失。

题目分为四个层次。第一层是抽样检测问题，需要在检测次数尽可能少的前提下判断供应商零配件批次是否应接收或拒收。第二层是已知次品率下的两零配件生产决策，需要对表1六种成本和次品率情形给出具体策略。第三层将生产结构推广到多个零配件、多个半成品和多道工序，需要给出可扩展的全流程决策模型。第四层进一步指出次品率本身来自抽样估计，因此需要考虑参数不确定性对第二、第三问决策结果的影响。

本文的核心思路是把“是否检测、是否拆解”全部转化为可计算的离散决策变量，把“检测费用、购买费用、装配费用、调换损失和拆解费用”统一写入单位产品期望利润函数，再通过枚举、递推和后验扰动得到稳定、可解释的决策结果。

## 2 模型假设

1. 同一批次内零配件的质量状态相互独立，不同零配件之间的质量状态也相互独立。
2. 检测过程能够准确识别不合格品，且检测本身不会改变零配件或成品的质量状态。
3. 若零配件被检测，则检测出的不合格件直接丢弃；为获得一个可用于装配的合格零配件，需要考虑重复采购或等效补足成本。
4. 拆解不会损坏可回收的零配件或半成品，但拆解需要支付固定费用。
5. 若成品不检测，则产品直接进入市场；不合格成品进入市场后会产生调换损失。
6. 问题三中，半成品层和成品层均可用“有效成本 + 通过概率”的形式递推。
7. 问题四中，题目给定的次品率被视为抽样估计的中心值，本文用等效样本量80构造Beta后验分布进行扰动。

## 3 符号说明

| 符号 | 含义 |
|---|---|
| $p_i$ | 第 $i$ 类零配件、半成品或成品的次品率 |
| $c_i$ | 第 $i$ 类零配件购买单价 |
| $t_i$ | 第 $i$ 类零配件或产品检测成本 |
| $a$ | 装配成本 |
| $s$ | 成品市场售价 |
| $L$ | 不合格成品流入市场后的调换损失 |
| $r$ | 拆解费用 |
| $d_i$ | 是否检测的0-1决策变量 |
| $z$ | 是否拆解的0-1决策变量 |
| $q$ | 最终好品概率 |
| $\\Pi$ | 单位产品期望利润 |

## 4 问题一：抽样检测模型

设抽样数量为 $n$，其中观测到的次品数为 $X$。在真实次品率为 $p$ 时，$X$ 服从二项分布：

$$
X\\sim B(n,p),\\quad P(X=x)=\\binom{{n}}{{x}}p^x(1-p)^{{n-x}}.
$$

拒收情形要求“在95%的信度下认定次品率超过标称值”，因此应使用单侧下置信界。若观测到的次品数较多，使得次品率的95%单侧下置信界大于 $p_0=0.10$，则拒收该批零配件。接收情形要求“在90%的信度下认定次品率不超过标称值”，因此应使用单侧上置信界。若观测到的次品数足够少，使得次品率的90%单侧上置信界不超过 $p_0$，则接收该批零配件。

为满足“检测次数尽可能少”的要求，本文从 $n=1$ 开始枚举样本量，并对每个 $n$ 枚举阈值 $x$。一旦发现满足置信界要求的组合，即记录为最小样本量方案。得到结果如下。

{md_table(q1, ['scenario', 'n', 'threshold', 'one_sided_bound', 'interpretation'], 2)}

![图1 问题一抽样方案操作特性曲线](figures/fig_q1_oc_curves.png)

图1展示了不同真实次品率下接收和拒收规则的触发概率。拒收规则只需要2件样本且2件均为次品时触发，这是因为该情形已经足以把次品率的单侧下界推高到10%以上。接收规则更严格，需要连续抽检22件且未发现次品，才能使90%单侧上界降至10%以下。这一结果说明：在低次品率批次中，“证明不超过10%”通常比“发现明显超过10%”需要更多样本。

## 5 问题二：两零配件生产决策模型

问题二包含四个二元决策：是否检测零配件1、是否检测零配件2、是否检测成品、是否拆解不合格成品。对每一种情形共有 $2^4=16$ 种策略。本文对每种策略计算单位产品期望利润，并选取最大值。

若某零配件被检测，则不合格件会被剔除。为了获得一个进入装配环节的合格零配件，等效期望成本为

$$
C_i^*=\\frac{{c_i+t_i}}{{1-p_i}}.
$$

若某零配件不检测，则其直接进入装配，成本为 $c_i$，合格概率为 $1-p_i$。设成品装配次品率为 $p_f$，两个输入零配件进入装配后的合格概率分别为 $g_1,g_2$，则最终成品合格概率为

$$
q=g_1g_2(1-p_f).
$$

若成品不检测，则产品直接售出，期望利润需要扣除不合格品导致的调换损失。若成品检测，则只有检测合格的成品进入市场，不合格成品可以丢弃或拆解。拆解收益按不合格成品中仍然合格的零配件期望价值计算。完整利润函数可写为

$$
\\Pi = E(\\text{{销售收入}})-E(\\text{{采购与检测成本}})-E(\\text{{装配成本}})-E(\\text{{调换损失}})+E(\\text{{拆解净回收}}).
$$

六种情形的最优策略如下。

{md_table(q2, ['scenario', 'policy', 'expected_profit', 'defect_risk', 'baseline_profit', 'improvement'], 6)}

![图2 问题二六种情形的最优策略收益对比](figures/fig_q2_profit_comparison.png)

从表中可以看到，情况1、2、3、5的最优策略均是不检测零配件、不检测成品但拆解不合格品。这是因为在这些情形下检测成本相对较高，而拆解费用为5元，拆解仍可回收一定价值。情况4由于零配件检测成本降低且调换损失较高，模型选择检测零配件1以降低后续缺陷风险。情况6的拆解费用为40元，远高于可回收价值，因此最优策略为不拆解。

这个结果也说明，检测并不是越多越好。若检测费用高于它能减少的期望损失，检测会降低利润；若拆解费用高于回收价值，拆解也会变成负收益操作。因此，生产决策必须放到完整成本结构中比较，而不能只根据次品率高低做经验判断。

## 6 问题三：多工序多零配件模型

问题三包含8个零配件、3个半成品和1个成品。若直接在所有层面展开质量状态，状态数会迅速膨胀。本文采用分层递推：先对每个半成品块计算其有效成本和通过概率，再把半成品作为成品层的输入。

对于某个半成品，设其由若干零配件组成。每个零配件根据是否检测产生不同的有效成本和合格概率。半成品装配后还有自身次品率，因此半成品好品概率为

$$
q_h=\\prod_i g_i(1-p_h).
$$

若半成品被检测，则只有合格半成品进入下一层；不合格半成品根据拆解决策产生回收价值或直接丢弃。于是半成品对下一层的贡献可以压缩为两个量：有效成本 $C_h^*$ 与通过概率 $q_h^*$。成品层再用相同思想计算最终好品概率和期望利润。

全流程决策变量包括8个零配件检测决策、3个半成品检测决策、3个半成品拆解决策、成品检测决策和成品拆解决策，共16个二元变量。本文枚举 $2^{{16}}=65536$ 个策略。最优策略为：

```text
{q3_best['policy']}
```

该策略的单位产品期望利润为 {q3_best['expected_profit']:.2f} 元/件，成品最终缺陷风险为 {q3_best['defect_risk']:.4f}。其中“零检=00011000”表示只检测第4、第5个零配件；“半检=000”表示三个半成品均不检测；“半拆=001”表示只对第三个半成品的不合格结果保留拆解；成品层不检测但保留拆解。该策略反映出：在售价较高而检测成本存在时，模型并不会盲目检测所有环节，而是选择对成本收益比更高的局部环节进行控制。

![图3 问题三前十个策略的期望利润](figures/fig_q3_top_policy_profit.png)

![图4 问题三最优策略的检测环节](figures/fig_q3_policy_breakdown.png)

图3显示前十个策略的收益差异并不完全相同，最优策略具有明确收益优势。图4则把检测决策拆开展示，便于生产管理者直接理解哪些环节需要检测，哪些环节可以省略检测。

## 7 问题四：抽样误差下的稳健决策

问题四的关键变化是：问题二和问题三中的次品率不是确定已知，而是来自抽样检测。抽样估计会带来误差，如果仍用名义次品率直接决策，可能低估缺陷风险。本文采用Beta后验分布描述次品率不确定性。设题中给定次品率为 $p_0$，等效样本量为 $N=80$，则取

$$
p\\sim Beta(Np_0+1,N(1-p_0)+1).
$$

对每个候选策略，重复抽样次品率并计算期望利润，得到该策略的收益分布。稳健目标不取均值最大，而取10%分位收益最大。这样可以避免某些策略在平均情况下看似较好，但在次品率偏高时收益大幅下滑。

问题二稳健重算结果如下。

{md_table(q4_q2, ['scenario', 'policy', 'mean_profit', 'p10_profit', 'p90_profit'], 6)}

![图5 问题四抽样误差下的稳健策略收益区间](figures/fig_q4_robust_profit_interval.png)

对问题三，本文取名义收益前50个策略作为候选集，再进行后验扰动下的稳健筛选。稳健候选策略前三项如下。

{md_table(q4_q3, ['policy', 'mean_profit', 'p10_profit', 'p90_profit'], 3)}

从结果可以看出，问题二的稳健策略与名义策略整体保持一致，说明表1中的最优策略对中等规模抽样误差较稳定。但在情况4这类缺陷率和调换损失较高的场景中，收益下界明显低于均值，说明企业应增加抽样量或提高检测保守性。对问题三而言，多工序结构使误差传播更明显，因此在正式生产中应优先对关键零配件和半成品环节追加抽样。

## 8 模型检验

本文从三个角度进行模型检验。

第一，抽样方案检验。问题一不仅给出样本量和阈值，还绘制操作特性曲线，检查不同真实次品率下规则触发概率是否符合直觉。若真实次品率高于10%，拒收概率应随次品率上升而上升；若真实次品率低于10%，接收概率应更高。

第二，策略最优性检验。问题二完整枚举16种策略，问题三完整枚举65536种策略。由于策略空间已经全部遍历，最优策略不是由启发式搜索偶然得到，而是在模型假设下的全局最优。

第三，不确定性检验。问题四把次品率扰动后重新计算策略收益，观察策略在低分位收益下是否仍然稳定。这一检验能发现名义最优但风险暴露较大的策略。

## 9 灵敏度分析

从结果可以归纳出三类灵敏度规律。首先，调换损失越高，成品检测越有价值。因为不合格品流入市场后的损失越大，提前检测成品能够避免更高的期望损失。其次，检测成本越高，零配件检测越容易被放弃。若零配件检测费用高于它减少的后续损失，检测会直接降低单位产品利润。第三，拆解费用越高，拆解决策越容易转为丢弃。情况6正是典型例子，拆解费用40元使拆解净收益为负，因此模型选择不拆解。

对多工序结构而言，灵敏度还表现为误差累积。单个零配件次品率的小幅变化，会通过半成品和成品层级被放大。因此，在多工序生产中，企业不应只关注最终成品检测，还应识别对最终收益影响最大的关键零配件或半成品环节。

## 10 结论

本文建立了一个可复现的生产过程决策模型，并给出如下结论。

1. 对供应商批次，二项分布单侧置信界可以直接构造接收和拒收规则。标称次品率10%时，拒收规则样本量很小，但接收规则需要更长的“零次品”证据。
2. 对两零配件情形，是否检测和是否拆解取决于检测成本、调换损失、拆解费用和可回收价值的综合比较，而不是单独由次品率决定。
3. 对多工序情形，分层递推可以有效降低建模复杂度，并保持策略解释性。问题三中最优策略只检测部分关键零配件，说明全检未必经济。
4. 当次品率来自抽样估计时，应使用稳健收益准则重新评估策略。低分位收益能更好反映生产风险。

## 11 模型优缺点与改进方向

模型优点在于：第一，所有决策变量均为实际生产可执行动作，结果容易解释；第二，问题二和问题三均使用完整枚举，最优性清楚；第三，问题四把抽样误差显式写入决策流程，避免把估计值误当成真值。

模型局限在于：第一，拆解后的零配件循环利用采用期望回收近似，没有展开为无限期马尔可夫链；第二，没有考虑批量库存和生产节拍约束；第三，抽样检测默认无误判，未考虑检测设备的灵敏度和特异度。后续可把返修循环扩展为马尔可夫决策过程，并把库存约束、产能约束和检测误差同时纳入模型。

## 附录：代码与证据位置

- 建模代码：`paper_output/code/modeling/b_problem_modeling.py`
- Word 生成代码：`paper_output/code/modeling/build_report_docx.py`
- 模型结果：`paper_output/results/model_results.json`
- 指标文件：`paper_output/results/metrics.json`
- 结论文件：`paper_output/results/conclusions.json`
- 表格索引：`paper_output/tables/table_index.json`
- 图表索引：`paper_output/figure_index.json`
"""

    (OUT / "final_paper.md").write_text(report, encoding="utf-8")
    print(f"已扩写 Markdown：{OUT / 'final_paper.md'}")
    print(f"字符数：{len(report)}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
